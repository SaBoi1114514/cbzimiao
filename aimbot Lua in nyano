local MiaoLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/SaBoi1114514/cbzimiao/refs/heads/main/README.md')))()

MiaoLib:MakeNotification({
	Name = "The script is being opened",
	Content = "Script loading...",
	Image = "rbxthumb://type=Asset&id=5107182114&w=150&h=150",
	Time = 2
})

MiaoLib:MakeNotification({
	Name = "ALN(beta)",
	Content = "Script loading...",
	Image = "rbxthumb://type=Asset&id=5107182114&w=150&h=150",
	Time = 4
})

print("Anti-AFK is on")
		local vu = game:GetService("VirtualUser")
		game:GetService("Players").LocalPlayer.Idled:connect(function()
		   vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
		   wait(1)
		   vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
		end)		
local Window = MiaoLib:MakeWindow({Name = "ALN(Beta)",HidePremium = false,SaveConfig = true,IntroText = "Aimbot Luas in Nyano(Beta)",ConfigFolder = "Aimbot Luas in Nyano(Beta)"})
local statement = Window:MakeTab({
	Name = "Script details",
	Icon = "rbxassetid://14359155637",
	PremiumOnly = false
})

statement:AddParagraph("Your script executor:"," "..identifyexecutor().."")

statement:AddParagraph("The ID of the server you are on:"," "..game.GameId.."")

statement:AddLabel("Welcome to ALN scripts")

local FE = Window:MakeTab({Name = "Player parameters",Icon = "rbxassetid://14481493145",PremiumOnly = false})

FE:AddToggle({
	Name = "night vision",
	Default = false,
	Callback = function(Value)
		if Value then
			game.Lighting.Ambient = Color3.new(1,1,1)
		else
			game.Lighting.Ambient = Color3.new(0,0,0)
		end
	end
})

FE:AddSlider({
    Name = "velocity",
    Min = 0,
    Max = 512,
    Default = false,
    Color = Color3.fromRGB(5,50,255),
    Increment = 1,
    ValueName = "Value",
    Callback = function(Value)
		game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
end})

FE:AddSlider({
    Name = "jump",
    Min = 0,
    Max = 512,
    Default = false,
    Color = Color3.fromRGB(5,50,255),
    Increment = 1,
    ValueName = "Value",
    Callback = function(Value)
		game.Players.LocalPlayer.Character.Humanoid.JumpPower = Value
end})

FE:AddSlider({
	Name = "gravity",
	Min = 0,
	Max = 512,
	Default = false,
	Color = Color3.fromRGB(5,50,255),
	Increment = 1,
	ValueName = "Value",
	Callback = function(Value)
		game.Workspace.Gravity = Value
end})

FE:AddSlider({
	Name = "Play Fov",
	Min = 0,
	Max = 120,
	Default = false,
	Color = Color3.fromRGB(5,50,255),
	Increment = 1,
	ValueName = "Value",
	Callback = function(Value)
	game.Workspace.CurrentCamera.FieldOfView = Value
end})

MiaoLib:Init()
